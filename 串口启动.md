换臂后 USB 设备在 Windows 侧重新枚举了，之前的 attach 已失效，需要重新转发一次。按顺序来：

**Windows 侧（管理员 PowerShell）**：

```powershell
usbipd list
```

在输出里找新臂的串口设备（描述里带 reBot/Seeed，或 VID `2e88` 那类；看它对应的 **BUSID**，形如 `1-3`）。然后：

```powershell
usbipd bind --busid 2-4
usbipd attach --wsl --busid 2-4
```

（如果 `list` 里该设备已显示 `Bound`，跳过 bind 直接 attach。）

**WSL 侧**：

```bash
dmesg | tail -5          # 看 cdc_acm 注册了 ttyACM? 还是 ftdi_sio 注册了 ttyUSB?
ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null
sudo chmod 666 /dev/ttyACM0
```

两种情况：

- 出现 `/dev/ttyACM0` → 直接重跑 `python scripts/00_check_bus.py`；
- 出现的是 `/dev/ttyUSB0`（新臂换了串口桥芯片）→ 告诉我，我把 yaml 的 `channel` 和脚本里的路径改成 ttyUSB0（或做个 udev 软链）。

把 `usbipd list` 里新臂那行和 `dmesg | tail -5` 的输出贴我。


连接motorbridge
```bash
export MOTORBRIDGE_WS_TOKEN=123456
motorbridge-gateway --bind 0.0.0.0:9002 --vendor damiao --transport dm-serial --serial-port /dev/ttyACM0 --serial-baud 921600 --dt-ms 20
```


网页控制项目启动
终端1
cd ~/Projects/HyperdualQuaternions_modeling_robotic/Borot-Arm_Mujoco
./rebotarm start dm
终端2
cd ~/Projects/HyperdualQuaternions_modeling_robotic/Borot-Arm_Mujoco
./rebotarm start web

llm语言模型控制启动
export REBOTARM_LLM_BASE_URL="https://open.bigmodel.cn/api/paas/v4"
export REBOTARM_LLM_MODEL="glm-5.3-flash"   # 或 glm-4-air / glm-4-plus
export REBOTARM_LLM_API_KEY="d81c4c708edf441eab649f9021ac76ee.pK0ctk6RT1FYtXKb"
./scripts/start_rebotarm_text_agent_http.sh

cd ~/Projects/HyperdualQuaternions_modeling_robotic/Borot-Arm_Mujoco/reBotArm_ros2_DM
source scripts/source_rebotarm_env.sh
ros2 launch rebotarm_agent rebotarm_mcp.launch.py
ros2 launch rebotarm_agent rebotarm_mcp.launch.py motion_mode:=allow


echo 'export ANTHROPIC_BASE_URL="https://chat.cloudapi.vip"' >> ~/.bash_profile
echo 'export ANTHROPIC_AUTH_TOKEN="sk-UXtx1TYCAGfmoMJ2eX7CxkmbZdkZkoBBiobNlNyhg5xQFm7b"' >> ~/.bash_profile
echo 'export ANTHROPIC_DEFAULT_SONNET_MODEL="claude-sonnet-5-code"' >> ~/.bash_profile
echo 'export ANTHROPIC_DEFAULT_OPUS_MODEL="claude-opus-5-code"' >> ~/.bash_profile
source ~/.bash_profile