% ---------------------------------------------------------------------
% 第 6 章 仿真验证
% ---------------------------------------------------------------------
\section{仿真验证}

\subsection{平台与任务}

仿真平台为 CoppeliaSim 中的 7-DoF KUKA LBR4+（力矩模式，控制周期 5 ms），名义动力学取自 \cite{Gaz14}。任务为抓取--搬运--水平圆周跟踪（半径 0.06 m，标准角速度 1.0 rad/s）；$t=2.5\,\text{s}$ 时 0.25 kg 未建模负载刚性附着于末端，构成持续偏差型扰动 $d_{\mathrm{ex}}$。对比三种控制器：C1（本文，式 \eqref{eq:5.2}）、C2（\cite{Ch20}，twist 误差与本文相同、位姿反馈取螺旋对数）、C3（\cite{P2} 经速度伺服桥接至加速度级）；三律共享轨迹、初始条件与名义模型，DC 刚度均配平至 80（闭环极点 $\{-4,-20\}$），以隔离结构差异。为核验标度律 \eqref{eq:5.9}，C1 另设 tuned/fast 两个增益档（未补偿 $1/4$ 折减的 base 档作对照）；另设高速（$\omega=2.5\,\text{rad/s}$）与编码器测量噪声两个应力工况。

\subsection{结果与分析}

\subsubsection{静态刚度标度律与扰动反演}

负载附着引起的瞬态跳变在约 $1.5\,\text{s}$ 内回落；取圆周稳态窗口（$t\ge 12.5\,\text{s}$）统计，两档残差为

\begin{center}
\begin{tabular}{@{}lccc@{}}
\toprule
档位 & $K_p=\mathrm{diag}(p_O I_3, p_T I_3)$ & $\|\mathcal T\|_{\mathrm{rms}}$ (m) & $\|\mathcal O\|_{\mathrm{rms}}$ \\
\midrule
tuned & $(320, 80)$ & $4.86\times10^{-3}$ & $4.27\times10^{-3}$ \\
fast  & $(720, 180)$ & $2.20\times10^{-3}$ & $1.93\times10^{-3}$ \\
\bottomrule
\end{tabular}
\end{center}

平移/旋转刚度同步提高 $2.25$ 倍，两通道残差同步降至 $1/2.21$（图~\ref{fig:sec6-gain-tiers}），与标度律 \eqref{eq:5.9} 的 $1/\rho$ 预言一致。由 \eqref{eq:5.9} 反演等效扰动
\begin{equation*}
\|d_v\| = k_{p,T}\|\mathcal T\|_{\mathrm{ss}}, \qquad \|d_\omega\| = \tfrac12\lambda(K_{p,O})\|\mathcal O\|_{\mathrm{ss}},
\end{equation*}
tuned 档得 $(\|d_v\|,\|d_\omega\|)=(0.389,\,0.683)$，fast 档得 $(0.396,\,0.696)$，两档一致至 $1.91\%$（图~\ref{fig:sec6-v1-inversion}）：两个独立增益档反演出同一物理扰动，定理 3(d) 的扰动模型与 $1/4$ 折减补偿在结构上得到确认。反例是 base 档（$K_p=16I_6$，未补偿折减，有效旋转刚度仅 4）：姿态误差放大至 tuned 档的 $12.3$ 倍，反演偏离 tuned--fast 均值 $35.5\%$/$39.0\%$，标度律失效——折减补偿的必要性由此从反面确认。

\subsubsection{应力工况下的控制器分化}

标准工况下三律位置残差差异 $<0.1\%$（静态刚度锁定），分化只在应力工况显现。速度级误差 $\|e_\xi\|_{\mathrm{rms}}$（$\times10^{-3}$）：

\begin{center}
\begin{tabular}{@{}lccc@{}}
\toprule
工况 & C1 & C2 & C3 \\
\midrule
标准 & 0.940 & 0.940 & 0.955 \\
高速 ($\omega=2.5$) & 2.314 & 2.314 & 2.361 \\
噪声 & 3.164 & 3.165 & 3.234 \\
\bottomrule
\end{tabular}
\end{center}

C1 与 C2 在全部工况下数值等价（相对差 $\le 0.05\%$）：二者共享同一信息集（解析前馈与 $\mathrm{Ad}$-搬运 twist 误差），位姿反馈整形（$A^\top$ 对螺旋对数）之差在近恒等域为高阶项。等价性说明 C1 的收益不在精度而在证书——定理 3 的耗散等式与各性能界对 $A^\top$ 整形成立，对螺旋对数不成立。C3 则在应力下系统性劣化（图~\ref{fig:sec6-v2-divergence}）：噪声下 $+2.2\%$ 来自桥接差分 $\Delta\dot q_{\mathrm{cmd}}/\Delta t$ 的噪声放大，高速下 $+2.0\%$ 源于一阶前馈无法解析补偿 $\omega^2$ 向心项；C1/C2 的解析前馈不受此限。

本节仿真承担实机无法承载的实验：无拴定环境下的标度律 \eqref{eq:5.9} 反演（实机准静态残差由固件拴定弹簧与摩擦主导）与注入式应力工况；证书链的实机核验（水平集余量、$\mathrm{cond}_2$ 条件与 $\gamma$ 阶梯）见 \S7。单次运行的 $1.5\%$--$2\%$ 相对差异不具统计显著性；大初始误差扫描（双覆盖分支、unwinding 规避与几何鲁棒性的定量评估）留作后续工作。

\begin{figure}[!tbp]
\centering
\includegraphics[width=0.88\textwidth]{../TNDQ_sim/results/sec6_c1_gain_tiers_load.pdf}
\caption{C1 两种增益档的稳态残差对比（带载标准工况）：(a)(b) 位置/姿态误差时序；(c)(d) 圆周稳态窗口内的 RMS。刚度提高 $2.25$ 倍使稳态残差降低约 $2.21$ 倍，符合标度律 \eqref{eq:5.9} 的线性预测。}
\label{fig:sec6-gain-tiers}
\end{figure}

\begin{figure}[!tbp]
\centering
\includegraphics[width=\textwidth]{../TNDQ_sim/results/sec6_v1_inversion_load.pdf}
\caption{静态刚度标度律 \eqref{eq:5.9} 的扰动反演一致性：(a) 三个独立增益档反演同一物理扰动 $d_{\mathrm{ex}}$ 的幅值；(b) 相对 tuned--fast 均值的偏差——tuned/fast 一致至 $\pm 1\%$ 以内，未补偿 1/4 旋转折减的 base 档偏离 $-35.5\%$/$-39.0\%$，标度律失效。}
\label{fig:sec6-v1-inversion}
\end{figure}

\begin{figure}[!tbp]
\centering
\includegraphics[width=\textwidth]{../TNDQ_sim/results/sec6_v2_certificate_divergence_load.pdf}
\caption{极限工况下的控制器分化：(a) 标准/高速/噪声三工况的稳态 twist 误差 RMS；(b) 相对 C1 的偏差——C2 与 C1 数值等价（$\le 0.05\%$），C3 系统性劣化（噪声下 $+2.2\%$、高速下 $+2.0\%$），与桥接差分和前馈结构缺陷的机制一致。}
\label{fig:sec6-v2-divergence}
\end{figure}
